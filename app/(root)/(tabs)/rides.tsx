import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-3xl text-red-500">Rides</Text>
      </View>
    </SafeAreaView>
  );
};
export default Rides;
