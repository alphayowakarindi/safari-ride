import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Onboarding = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-3xl text-red-500">Onboarding </Text>
      </View>
    </SafeAreaView>
  );
};
export default Onboarding;
