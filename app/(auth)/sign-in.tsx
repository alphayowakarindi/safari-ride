import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignIn = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-3xl text-red-500">SignIn </Text>
      </View>
    </SafeAreaView>
  );
};
export default SignIn;
